import subprocess

def run_testdisk_tool():
    print("Running TestDisk Tool...")
    print("=======================")
    
    # Call the TestDisk command-line tool (replace this with the actual command)
    testdisk_command = ["testdisk"]
    subprocess.run(testdisk_command)

if __name__ == "__main__":
    run_testdisk_tool()
