import subprocess

def run_photorec_tool():
    print("Running PhotoRec Tool...")
    print("=======================")
    
    # Call the PhotoRec command-line tool (replace this with the actual command)
    photorec_command = ["photorec"]
    subprocess.run(photorec_command)

if __name__ == "__main__":
    run_photorec_tool()
